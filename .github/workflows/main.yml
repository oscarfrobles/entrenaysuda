on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]
jobs:
  heroku_git_deploy_job:
      runs-on: ubuntu-latest

      steps:
      - run: heroku login && heroku run bash --app trainandsweat