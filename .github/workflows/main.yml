name: Django CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.8.12]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      env:
        SENTRY_DSN: 'https://16d176ee83c64a0f903c7a413e4cc2b8@o1038060.ingest.sentry.io/6006251'
        OAUTH_CLIENT_ID: '426570479868-h9gncmdlrsgu3is3u8umjcf09h4l6mre.apps.googleusercontent.com'
        OAUTH_APP_SECRET_KEY: '2-umuj-pyRoyQJn-9rGWLm_O'
        DB_USER: cibeles
        DB_PASSWORD: 123456
        DB_HOST: localhost
        DB_NAME: entrenamientos
        DB_PORT: 5432
        DJANGO_SECRET_KEY: 'django-insecure-=edo#v%a+)e_gjkjjxqojk9tnw)*1l%9ebn_rle3vl5z+$e1_j'
      run: |
        python manage.py test
